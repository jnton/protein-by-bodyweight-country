const DATA_URL="./data/protein_bodyweight_country_year.json";const metricDefinitions={protein_supply_g_kg_day:{label:"Protein supply per kg",unit:"g/kg/day",decimals:2,colorScale:[[0,"#fff4d6"],[.45,"#ffb06f"],[1,"#c8382e"]]},protein_supply_g_day:{label:"Protein supply per person",unit:"g/person/day",decimals:1,colorScale:[[0,"#e5f5f2"],[.5,"#5fb8aa"],[1,"#145d56"]]},estimated_adult_bodyweight_kg:{label:"Estimated adult bodyweight",unit:"kg",decimals:1,colorScale:[[0,"#eef0ff"],[.5,"#9099d8"],[1,"#343b79"]]}};const state={records:[],metadata:{},metric:"protein_supply_g_kg_day",year:2016,selectedCodes:["ITA","USA","JPN","IND","NGA"]};const el=id=>document.getElementById(id),isDark=()=>document.documentElement.dataset.theme==="dark",chartBg=()=>isDark()?"#111e1b":"#fffdf8",chartText=()=>isDark()?"#edf4ef":"#10231f",chartGrid=()=>isDark()?"rgba(237,244,239,.12)":"rgba(16,35,31,.12)";function formatValue(value,definition=metricDefinitions[state.metric]){return value==null||Number.isNaN(Number(value))?"—":`${Number(value).toFixed(definition.decimals)} ${definition.unit}`}function percentile(values,p){const sorted=values.filter(Number.isFinite).sort((a,b)=>a-b);if(!sorted.length)return null;const index=(sorted.length-1)*p,lower=Math.floor(index),upper=Math.ceil(index);return lower===upper?sorted[lower]:sorted[lower]+(sorted[upper]-sorted[lower])*(index-lower)}function plotConfig(){return{responsive:true,displaylogo:false,modeBarButtonsToRemove:["lasso2d","select2d"]}}function commonLayout(){return{paper_bgcolor:chartBg(),plot_bgcolor:chartBg(),font:{family:"Inter, system-ui, sans-serif",color:chartText()},margin:{l:54,r:20,t:18,b:50},hoverlabel:{font:{family:"Inter, system-ui, sans-serif"}}}}function recordsForYear(year,requireMetric=true){return state.records.filter(row=>Number(row.Year)===Number(year)&&(!requireMetric||row[state.metric]!=null))}function renderSummary(){const normalized=state.records.filter(row=>row.protein_supply_g_kg_day!=null),years=[...new Set(normalized.map(row=>Number(row.Year)))].sort((a,b)=>a-b),countries=new Set(normalized.map(row=>row.Code));el("country-count").textContent=countries.size.toLocaleString();el("record-count").textContent=state.records.length.toLocaleString();el("year-range").textContent=years.length?`${years[0]}–${years.at(-1)}`:"—";const slider=el("year-slider");slider.min=String(years[0]);slider.max=String(years.at(-1));state.year=Number(years.at(-1));slider.value=String(state.year);el("year-output").value=String(state.year)}function populateCountrySelect(){const countryMap=new Map;state.records.forEach(row=>{row.Code&&row.Entity&&countryMap.set(row.Code,row.Entity)});const options=[...countryMap.entries()].sort((a,b)=>a[1].localeCompare(b[1])),select=el("country-select");select.innerHTML=options.map(([code,name])=>`<option value="${code}" ${state.selectedCodes.includes(code)?"selected":""}>${name}</option>`).join("")}function renderMap(){const rows=recordsForYear(state.year),definition=metricDefinitions[state.metric],values=rows.map(row=>Number(row[state.metric])).filter(Number.isFinite),zmin=percentile(values,.03),zmax=percentile(values,.97);el("map-title").textContent=`${definition.label}, ${state.year}`;el("map-coverage").textContent=`${rows.length} countries`;const trace={type:"choropleth",locationmode:"ISO-3",locations:rows.map(row=>row.Code),z:rows.map(row=>row[state.metric]),text:rows.map(row=>row.Entity),customdata:rows.map(row=>[row.protein_supply_g_day,row.estimated_adult_bodyweight_kg,row.estimate_status]),colorscale:definition.colorScale,zmin,zmax,marker:{line:{color:isDark()?"#233b36":"#f5f1e8",width:.45}},colorbar:{title:{text:definition.unit},thickness:12,outlinewidth:0},hovertemplate:`<b>%{text}</b><br>${definition.label}: %{z:.${definition.decimals}f} ${definition.unit}<br>Protein supply: %{customdata[0]:.1f} g/person/day<br>Adult bodyweight proxy: %{customdata[1]:.1f} kg<extra></extra>`},layout={...commonLayout(),margin:{l:0,r:0,t:0,b:0},geo:{projection:{type:"natural earth"},showframe:false,showcoastlines:false,showland:true,landcolor:isDark()?"#1b2d29":"#ebe6db",bgcolor:chartBg()}};Plotly.react("map-chart",[trace],layout,plotConfig())}function renderRanking(){const definition=metricDefinitions[state.metric],rows=recordsForYear(state.year).sort((a,b)=>Number(b[state.metric])-Number(a[state.metric])).slice(0,12);el("ranking-list").innerHTML=rows.map((row,index)=>`<li><span class="rank-number">${String(index+1).padStart(2,"0")}</span><span class="rank-name" title="${row.Entity}">${row.Entity}</span><span class="rank-value">${formatValue(row[state.metric],definition)}</span></li>`).join("")}function renderTrend(){const definition=metricDefinitions[state.metric],selected=new Set(state.selectedCodes),rows=state.records.filter(row=>selected.has(row.Code)&&row[state.metric]!=null),byCountry=Map.groupBy?Map.groupBy(rows,row=>row.Code):rows.reduce((map,row)=>(map.has(row.Code)||map.set(row.Code,[]),map.get(row.Code).push(row),map),new Map),traces=[...byCountry.values()].map(countryRows=>{const sorted=countryRows.sort((a,b)=>Number(a.Year)-Number(b.Year));return{type:"scatter",mode:"lines",name:sorted[0].Entity,x:sorted.map(row=>row.Year),y:sorted.map(row=>row[state.metric]),line:{width:2.4},hovertemplate:`<b>${sorted[0].Entity}</b><br>%{x}: %{y:.${definition.decimals}f} ${definition.unit}<extra></extra>`}});el("trend-title").textContent=`${definition.label} over time`;const layout={...commonLayout(),xaxis:{title:"Year",gridcolor:chartGrid(),zeroline:false},yaxis:{title:definition.unit,gridcolor:chartGrid(),zeroline:false},legend:{orientation:"h",y:1.14,x:0}};Plotly.react("trend-chart",traces,layout,plotConfig())}function renderScatter(){const rows=recordsForYear(state.year,false).filter(row=>row.protein_supply_g_day!=null&&row.estimated_adult_bodyweight_kg!=null),trace={type:"scatter",mode:"markers",x:rows.map(row=>row.estimated_adult_bodyweight_kg),y:rows.map(row=>row.protein_supply_g_day),text:rows.map(row=>row.Entity),customdata:rows.map(row=>row.protein_supply_g_kg_day),marker:{size:9,opacity:.78,color:rows.map(row=>row.protein_supply_g_kg_day),colorscale:metricDefinitions.protein_supply_g_kg_day.colorScale,line:{width:.5,color:chartBg()},showscale:false},hovertemplate:"<b>%{text}</b><br>Bodyweight proxy: %{x:.1f} kg<br>Protein supply: %{y:.1f} g/day<br>Normalized: %{customdata:.2f} g/kg/day<extra></extra>"},layout={...commonLayout(),xaxis:{title:"Estimated adult bodyweight (kg)",gridcolor:chartGrid(),zeroline:false},yaxis:{title:"Protein supply (g/person/day)",gridcolor:chartGrid(),zeroline:false}};Plotly.react("scatter-chart",[trace],layout,plotConfig())}function renderAll(){el("year-output").value=String(state.year);renderMap();renderRanking();renderTrend();renderScatter()}function wireEvents(){el("metric-select").addEventListener("change",event=>{state.metric=event.target.value;renderAll()});el("year-slider").addEventListener("input",event=>{state.year=Number(event.target.value);renderAll()});el("country-select").addEventListener("change",event=>{state.selectedCodes=[...event.target.selectedOptions].map(option=>option.value).slice(0,8);renderTrend()});el("theme-toggle").addEventListener("click",()=>{document.documentElement.dataset.theme=isDark()?"light":"dark";localStorage.setItem("protein-theme",document.documentElement.dataset.theme);renderAll()})}async function init(){const savedTheme=localStorage.getItem("protein-theme");savedTheme&&(document.documentElement.dataset.theme=savedTheme);try{const response=await fetch(DATA_URL);if(!response.ok)throw new Error(`HTTP ${response.status}`);const payload=await response.json();state.records=payload.records;state.metadata=payload.metadata;renderSummary();populateCountrySelect();wireEvents();renderAll()}catch(error){console.error(error);el("error-state").hidden=false}}window.addEventListener("DOMContentLoaded",init);
